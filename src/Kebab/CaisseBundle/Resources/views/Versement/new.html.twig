{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Versement creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('versement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
