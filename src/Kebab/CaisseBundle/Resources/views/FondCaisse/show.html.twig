{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FondCaisse</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Solde</th>
                <td>{{ entity.solde }}</td>
            </tr>
            <tr>
                <th>Achats</th>
                <td>{{ entity.achats }}</td>
            </tr>
            <tr>
                <th>Versements</th>
                <td>{{ entity.versements }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fondcaisse') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('fondcaisse_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
